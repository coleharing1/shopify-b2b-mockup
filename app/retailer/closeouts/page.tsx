"use client"

import { useState, useEffect } from "react"
import { AuthenticatedLayout } from "@/components/layout/authenticated-layout"
import { Tag, Clock, AlertTriangle, Zap, TrendingDown, Lock, Unlock, Timer, Percent } from "lucide-react"
// ... (rest of imports)

// ... (component logic)

export default function CloseoutsPage() {
  // ... (component logic)

  return (
    <AuthenticatedLayout>
    <div className="container mx-auto py-6 space-y-6">
        {/* ... (rest of JSX) */}
      </div>
    </AuthenticatedLayout>
  )
}
